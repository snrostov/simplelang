if 0 < N
  then
    if 1 < N
      then
        if 2 < N
          then
            if 3 < N
              then
                if 4 < N
                  then
                    if 5 < N
                      then f(6, N, "number 0, number 1, number 2, number 3, number 4, number 5, ")
                      else "number 0, number 1, number 2, number 3, number 4, "

                  else "number 0, number 1, number 2, number 3, "

              else "number 0, number 1, number 2, "

          else "number 0, number 1, "

      else "number 0, "

  else ""