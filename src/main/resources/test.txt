fun recursive(i, max) {
    result = if (i < max) "number $i, " + test_recursive(i + 1, max) else ""
}

fun recursive2(i, max, result) {
    result = if (i < max) test_recursive2(i + 1, max, result + "number $i, ") else result
}

fun cycle(n) {
  var i
  while (i < n) {
    result = result + "number " + i + ","
    i = i + 1
  }
}